# README and docs
README.md
LICENSE

# AstroNvim is installed via script, not managed by chezmoi
.config/nvim/**

# macOS only files (ignore on Linux)
{{- if not (eq .chezmoi.os "darwin") }}
.Brewfile.mac
{{- end }}
